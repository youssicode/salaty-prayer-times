//? DOM Elements Query Functions
//==============================

export const query = (selector) => document.querySelector(selector)
export const queryAll = (selector) => document.querySelectorAll(selector)
